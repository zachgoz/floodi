import React from 'react';
import Tab2 from './Tab2';

/**
 * Renders the real FloodCast dashboard with the first-run tour overlay active.
 */
const Intro: React.FC = () => <Tab2 showIntroTour />;

export default Intro;
