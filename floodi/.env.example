# Firebase Web App configuration
#
# Find these in Firebase Console:
# Project Settings > General > Your apps > Web app

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:abcdef1234567890

# Note: Do not commit your real .env with secrets to version control.
# Copy this file to .env and fill in your values locally.

# After configuring your Web App, enable Authentication providers:
# - Email/Password
# - Anonymous (for guest access and conversion to registered accounts)

# Firestore
# - Firestore is automatically initialized by the app when Firebase config is set.
# - You must enable Firestore in Firebase Console:
#   Console > Build > Firestore Database > Create database
#   Choose Production mode and a region close to your users.
# - Remember to deploy security rules and indexes (requires Firebase CLI):
#   firebase deploy --only firestore
# - See firestore.rules and firestore.indexes.json for this project’s configuration.
